name: Test

on:
  push:
    branches: [ "main" ]
  pull_request:
    paths:
      - 'src/**.rs'
env:
  CARGO_TERM_COLOR: always

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
    - name: Run tests
      run: cargo test --verbose
